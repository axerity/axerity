node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Ephemeral mount points — populated from the tracked defaults (src/content/demo,
# axerity.default.json) for the engine's own runs, or with a user's project by the
# CLI. Never tracked, so no run ever dirties the repo.
/src/content/docs
/axerity.json
build
/dist
# Copied from the root schema for the demo site
/static/axerity.schema.json

# axerity:openapi (generated)
src/content/docs/petstore-api
# end axerity:openapi

# npm pack artifacts
*.tgz

# Coverage reports
coverage
